# CHANGELOG.md — Historial de pasadas

> Registro cronológico, en una línea por pasada, de qué se agregó/cambió en cada sesión de trabajo sobre el proyecto. **No es la referencia técnica** — para saber CÓMO funciona algo hoy, leé el archivo de contexto del tema correspondiente (ver la tabla de "Cómo está dividido el contexto" en `PROJECT_CONTEXT.md`). Esto es solo trazabilidad histórica, pensado para abrirse rara vez.
>
> **Regla al agregar una pasada nueva**: UNA línea (dos como mucho si de verdad hace falta), no un párrafo. El detalle técnico del cambio va en el archivo de tema que corresponda (`CONTEXT_NUCLEO.md`, `CONTEXT_ENEMIGOS.md`, `CONTEXT_COMBATE.md`, `CONTEXT_TEMA.md`), no acá.

1. Sistema de "bonos condicionales" (badge naranja para daño no calculable, ej. Aura del Gran Panal de Nika), toggles con duración en turnos (Radiant Soul), fix de curación que no abría el panel de vida, modal "¿Qué puedo usar con mi X?", reorganización de `img/` en subcarpetas, Aredhel agregado como 7mo personaje jugable, soporte de Expertise, subida a nivel 8 de los 6 personajes originales.
2. Fixes de features automáticas de nivel 8 (Divine Strike de Leonidas, ASI extra de Guerrero en Lothar), segundo hechizo nuevo de Lunareth, equipo mágico de Aredhel, retrato real de Aredhel, click en el nombre también abre el modal de imagen de perfil.
3. Chiaragorn agregado como 8vo personaje jugable (copia exacta de Lunareth, a pedido).
4. Lyralei agregada como 9no personaje jugable (Pícara armada desde cero), placeholder de imagen genérico, y sección **Enemigos** nueva y completa (`enemigos.html`/`enemigo.html`/`enemigo.js`).
5. Pulido de Enemigos: selector de dado limitado a 4/6/8/10/12, "+ Más daño" (array `danos`), campo "Bono al d20", ícono como `select` curado, patrón de 2 modales (lista→detalle), sin `alert()`/`confirm()` nativos.
6. Corrección de un malentendido: las 5 secciones de Enemigos volvieron a mostrarse inline (no reemplazadas por el patrón de 2 modales); ese patrón pasó a ser el menú flotante de turno. Editar/Borrar por entrada, Acciones por turno, Acciones Legendarias, campo "consumo".
7. Importar enemigos pegando un JSON generado por una IA externa.
8. Reorganización de navegación: Enemigos pasó a vivir dentro de `extras.html`; `combate.html` usa "Enemigo existente" en vez de "Enemigo manual"; enemigo y combate comparten vida/CA en vivo vía la misma key de `localStorage`; botón 🔗 abre la ficha completa en pestaña nueva.
9. `enemigo.html`: las cards abren un modal de detalle con botón "Usar"; ningún modal se cierra clickeando afuera; Velocidad/Iniciativa/Acciones/Legendarias pasan a steppers ±1/±5; tile de Vida coloreado por %.
10. Fix de `claseColorVida()`: 50%-100% de vida ahora siempre pinta verde (antes solo pintaba a exactamente 100%).
11. `combate.js`: los últimos `alert()`/`confirm()`/`prompt()` nativos se reemplazan por modales propios; checkboxes de "agregar existente" restyleados.
12. Login simplificado a un solo nivel (`DM`/`boss1234` en `extras.html`); botón "Cerrar sesión"; Modal de Vida del Rastreador pasa a inputs de Daño/Curación + botón Aplicar (un solo registro de historial por evento).
13. Fix de `PERSONAJES_DISPONIBLES` (faltaban Aredhel/Chiaragorn/Lyralei); se descartan explícitamente tirador de dados y botón "deshacer"; fix de Puntos de Ki de Kael invisibles en el Rastreador; badge de turnos restantes de un toggle; bitácora de sesión; **tema oscuro/claro** en las 6 páginas principales.
14. Fix real de `statsFinales()` en `combate.js` (mejoras de raza/feat/ASI y salvaciones de equipo mal aplicadas); regla "ningún modal se cierra clickeando afuera" generalizada a todo el proyecto; historial marca aliado/rival y avisa "¡Llegó a 0!"; pips de salvación de muerte tocables; tile de Vida a 5 tramos de color; revisión completa de contraste en modo oscuro (sistema de variables CSS por color de identidad).
15. Bonos temporales de combate (Vida Máxima/CA) separados de las keys permanentes del personaje/enemigo; cambios de CA también en el historial; divisor visual de ronda; pips de salvación de muerte sin marcar en pastel.
16. Descanso corto/largo global para los 9 personajes de campaña (estén o no en el combate actual); regla homebrew de daño masivo / muerte instantánea.
17. **Doce correcciones puntuales en la ficha de Kael** (descripciones dinámicas vía placeholders, badge de costo en Ki, `oculto` extendido a equipo, fix del badge de CA en accesorios no-armadura, limpieza de texto) + **sistema de Vida Máxima por fórmula dinámica** (`vidaFormula`) aplicado a los 13 personajes activos, reemplazando el HP fijo; se confirmó que el Amuleto de la Salud solo sube CON (la Vida sube sola como efecto derivado) y se eliminó el mecanismo viejo `bonoVidaMaximaEquipado`. **En esta misma sesión se reestructuró la documentación**: `CONTEXT.md` (un solo archivo monolítico y creciente) se dividió en `CONTEXT_NUCLEO.md`, `CONTEXT_ENEMIGOS.md`, `CONTEXT_COMBATE.md`, `CONTEXT_TEMA.md` y este `CHANGELOG.md`, con `PROJECT_CONTEXT.md` como índice — ver la nota de proceso al inicio de `PROJECT_CONTEXT.md` y su tabla de "Cómo está dividido el contexto".
18. **Fix real de `vidaFormula`**: un `vidaMaxima` guardado en `localStorage` de antes de que existiera la fórmula (o de cualquier daño/curación posterior) quedaba pegado para siempre e ignoraba el recálculo — equipar el Amulet of Health de Kael no subía la Vida ni recargando la página ni descansando. Se agregó una key nueva (`vidaFormulaCalculada`) para distinguir "esto es solo lo último que calculó la fórmula" de "esto es un ajuste manual real hecho con los botones +/-", y la vida actual ahora sube/baja el mismo delta que el máximo (en vez de quedar pegada o saltar siempre a full) — ver `CONTEXT_NUCLEO.md` §6.
19. Los tiles de modificador de atributo y de salvación ahora abren un modal al hacer click (antes no hacían nada): el de modificador muestra el nombre del atributo en inglés/español + su valor real; el de salvación muestra el desglose del cálculo (mod + proficiencia + bono de equipo si aplica) — ver `CONTEXT_NUCLEO.md` §5.
